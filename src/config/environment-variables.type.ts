export type EnvironmentVariables = {
  NODE_ENV: string;
  PORT: string;
  TZ: string;
  DB_HOST: string;
  DB_PORT: string;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_DATABASE: string;
  DB_SYNCHRONIZE: string;
  JWT_SECRET: string;
  JWT_EXPIRES_IN: string;
};
